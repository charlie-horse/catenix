apiVersion: v2
name: missing-dep
description: Fixture chart declaring a dependency that isn't vendored.
version: 0.1.0
dependencies:
  - name: postgresql
    version: 16.0.0
    repository: https://charts.example.com
