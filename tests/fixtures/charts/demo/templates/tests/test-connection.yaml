apiVersion: v1
kind: Pod
metadata:
  name: {{ include "demo.fullname" . }}-test
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ["wget", "{{ include "demo.fullname" . }}:80"]
